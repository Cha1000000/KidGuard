package ru.homelab.kidguard.core.domain.model

/**
 * Контакт для экстренного звонка с ночного замка («Мама», «Папа»). Список ведут родители — оба
 * правят один список, он приезжает на устройство как часть политики. Ребёнок на замке может
 * позвонить только по этим номерам, набрать произвольный — нет.
 */
data class EmergencyContact(val name: String, val phone: String)
