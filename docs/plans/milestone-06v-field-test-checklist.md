# Веха 6В — чек-лист обкатки на реальном Tecno Spark 30 Pro (HiOS)

> Рабочий документ для полевого теста. Составлен 2026-07-16, до получения устройства.
> Родительский план — `milestone-06-hardening-field-test.md` (шаги 6.4 и 6.6).
> Части 6А (защита от обхода) и 6Б (glassmorphism) влиты в `main` — обкатываем собранную систему.

## Решения перед стартом (Володя, 2026-07-16)

- **Релизный keystore НЕ заводим.** Приложение ставится вручную в своей семье, не в Play. Release
  подписан debug-ключом, и это не делает сборку debuggable (`debuggable` — отдельный флаг типа
  сборки). SHA-1 debug-ключа уже зарегистрирован в Google OAuth — вход работает. Заведём ключ, если
  когда-нибудь понадобится Play; тогда не забыть добавить новый SHA-1 в Google Cloud Console, иначе
  Google-вход сломается.
- **Обкатываем на debug-сборке.** На HiOS ожидаются вендор-специфичные сюрпризы, а `Timber` посажен
  только под `BuildConfig.DEBUG` (`KidGuardApp.kt`) — на release диагностика была бы вслепую.
  Перейдём на release, когда сценарии стабильно пойдут.
- **Устройства:** детское — Tecno Spark 30 Pro (HiOS), родительское — второй реальный телефон
  (или эмулятор, если второго нет). Сервер — боевой AdminVPS, debug-сборка уже смотрит на него.

---

## Этап 0 — Разведка устройства (ДО тестов, 10 минут)

Цель — снять с HiOS факты, на которые опирается защита 6А. Команды проверены на эмуляторе.

> ### ✅ Частично выполнено 2026-07-16 на РОДИТЕЛЬСКОМ Tecno Володи
> Телефон Володи — тоже Transsion: **TECNO CM7, Android 16 (SDK 36), HiOS, PhoneMaster 6.2.2**,
> экран **1080x2436 @ 440dpi = 393dp** в ширину, навигация — жестовая, есть **рабочий профиль**
> (user 10) помимо основного (user 0).
> Это НЕ детский Spark 30 Pro (там Android 14), поэтому факты ниже надо перепроверить на детском —
> но вендорная специфика Transsion уже видна.
>
> | Что | Факт на TECNO CM7 (Android 16) | Вывод |
> |---|---|---|
> | Пакет настроек | `com.android.settings` (стандартный!), активность — **вендорная** `com.transsion.settings.homepage.SettingsHomeActivity` | Детект по заголовку окна выбран верно: по классу активности сломался бы |
> | Пакет инсталлера | `com.google.android.packageinstaller` (стандартный) | Хардкод бы выжил; резолв через PackageManager — страховка |
> | Вендорный автозапуск | **`com.transsion.phonemaster/com.cyin.himgr.autostart.AutoStartActivity` СУЩЕСТВУЕТ**, экспортирована, открывается → экран «Управление автозапуском» со списком приложений и мастер-тумблером «Управление всеми» | Первый кандидат в `VENDOR_AUTOSTART_COMPONENTS` верен → на детском Tecno карточка мастера, вероятно, откроет его сразу |
>
> Особенность устройства Володи: из-за рабочего профиля `pm list packages` без `--user 0` падает с
> SecurityException; ставить APK тоже нужно с `--user 0`. На детском такого не ожидается.
>
> ### ✅ Родительская часть обкатана на TECNO CM7 (2026-07-16)
> Детская часть (шаг 6.4 и большинство 6.6) **не проверялась** — на родительском телефоне
> foreground-сервис не стартует вообще (`BootReceiver` поднимает его только при роли `CHILD`).
>
> | Проверка | Результат |
> |---|---|
> | Google-вход на реальном устройстве | ✅ (Володя вошёл сам) |
> | Ребёнок подтянулся с боевого AdminVPS | ✅ Alina, статус «Привязан» |
> | **Иконки в Статистике = детские, не родительские** | ✅ **доказано**: `com.android.settings` установлен на Tecno со своей HiOS-иконкой, но показана AOSP-иконка эмулятора → иконка пришла с детского устройства. На двух эмуляторах эта проверка невозможна в принципе (наборы приложений совпадают) |
> | Список приложений в «Всегда доступные» | ✅ приложения эмулятора-ребёнка (Chrome, FinKeeper24, TMoble, YT Music), ни одного из 514 пакетов Tecno |
> | Синхронизация родитель → ребёнок | ✅ `+15 мин` с телефона → ребёнок «2 ч» → «2 ч 15 мин» за ~6 с (WS-push), чип «🎁 +15 мин от родителей» |
> | Синхронизация: отмена бонуса | ✅ доехала, чип пропал |
> | Градиент фона (фикс 6Б) | ✅ рисуется корректно, не схлопнут в угол |
> | DockBar скрыт на под-экранах Правил (фикс `showDockBar`) | ✅ |
> | **DockBar на 3-кнопочной навигации (фикс insets)** | ✅ стоит над системными кнопками, без наложения (режим временно переключался, возвращён на `gestural`) |
> | Светлая/тёмная темы на реальном экране | ✅ читаемы |
> | Перенос текста на узком экране (393dp) | ⚠️ найден и исправлен баг «Другое…» (см. коммит); прогон остальных экранов других переносов не выявил |

### 0.1 Пакеты настроек и инсталлера

```bash
# Пакет системных настроек
adb shell cmd package resolve-activity --brief -a android.settings.SETTINGS | tail -1
# Пакет, который показывает диалог удаления приложения
adb shell cmd package resolve-activity --brief -a android.intent.action.DELETE -d package:ru.homelab.kidguard | tail -1
```

**Что ожидаем:** на эмуляторе — `com.android.settings/...` и
`com.google.android.packageinstaller/...`.

**Зачем:** если на HiOS инсталлер свой (`com.transsion.*` или иной) — **защита от удаления не
сработает**, пока детект завязан на список констант. Задача «убрать хардкод пакетов» это чинит
(определять через `PackageManager`), но факт всё равно надо зафиксировать: он проверяет, что фикс
угадал правильно.

### 0.2 Заголовки критичных экранов

Детект в `KidGuardAccessibilityService` идёт **по заголовку окна** (`AccessibilityWindowInfo.title`),
а не по классу активности — это сделано кросс-вендорно. Но ключевые слова надо сверить с фактом.

> ### ✅ Выполнено 2026-07-18 на ДЕТСКОМ Tecno Spark 30 Pro (KL6, Android 14, HiOS 14.5)
> **Важно:** HiOS **режет Timber-логи** сторонних приложений в logcat (видны только системные
> записи процесса) — команда с `KidGuardA11y` на этом устройстве бесполезна. Заголовки снимались
> через `dumpsys accessibility` (строки `A11yWindow[...title=...]`) — работает.
>
> | Экран | Заголовок на KL6 | Детект |
> |---|---|---|
> | Спец. возможности | **«Доступность»** | ❌ была дыра → ключ добавлен (`bad6999`) |
> | VPN | «VPN» | ✅ |
> | Админы (список) | «Приложения для администрирования устройств» | ✅ («администрир») |
> | Карточка админа KidGuard | «Активировать приложение администратора устройства?» | ✅ |
> | Дата и время | «Дата и время» | ✅ |
> | О приложении KidGuard | **«KidGuard»** (= label, не «о приложении»!) | ❌ была дыра → детект расширен на label (`bad6999`) |
> | Хранилище приложения | **«Память»** | ❌ была дыра → ключ добавлен (`bad6999`) |
> | Диалог удаления | «KidGuard» + пакет инсталлера | ✅ |
>
> Перехват «Доступности» и «О приложении» после фиксов **проверен живьём** на устройстве
> (PIN-оверлей появляется, верный PIN 2468 пропускает). Ещё факт: `am start` интентом настроек
> HiOS часто поднимает старую задачу настроек с последним открытым экраном — для чистого теста
> нужен `force-stop com.android.settings` + флаг `0x10008000` (NEW_TASK|CLEAR_TASK).

Открыть на устройстве по очереди и записать заголовок (на HiOS — через
`dumpsys accessibility | grep title=`, logcat бесполезен, см. выше):

| Экран | Ожидаемое ключевое слово | Факт на HiOS |
|---|---|---|
| Настройки → Спец. возможности | `специальные возможности` / `доступность` / `accessibility` | «Доступность» ✅ |
| Настройки → VPN | `vpn` | «VPN» ✅ |
| Настройки → Администраторы устройства (список) | `администрир` | ✅ |
| То же → карточка KidGuard | `администратор` | ✅ |
| Диалог деактивации Device Admin | `kidguard` + `отключ` | не проверялся (Device Admin ещё не выдан) |
| Долгий тап по иконке → Удалить | label приложения | «KidGuard» (инсталлер) ✅ |

**Если заголовок не совпал** — дописать ключевое слово в соответствующий `*_KEYWORDS` в
`KidGuardAccessibilityService`, НЕ переходя на детект по классу активности (правило: универсально,
не под одну модель).

### 0.3 Вендорный автозапуск (главный риск)

У Transsion есть **отдельный** менеджер автозапуска/энергосбережения помимо стандартного
`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`. Он умеет убивать foreground-сервисы, стандартного API нет.

> ### ✅ Выполнено 2026-07-18 на детском KL6
> `com.transsion.phonemaster/com.cyin.himgr.autostart.AutoStartActivity` (компонент, подтверждённый
> ранее на CM7) **существует и открывается интентом** → экран «Управление автозапуском».
> **KidGuard в списке уже «Разрешено»** (тумблер включён) — руками ничего делать не пришлось.
> Карточка мастера разрешений на этом устройстве откроет нужный экран сразу.

- Найти в настройках HiOS: «Автозапуск» / «Диспетчер автозапуска» / «Управление питанием» /
  «Защищённые приложения» — назвать может по-разному.
- Включить для KidGuard **вручную**, записать точный путь в настройках → он пойдёт в текст
  пункта мастера разрешений.
- Проверить, открывается ли этот экран интентом (мастер попытается, с фолбэком на настройки
  приложения).

### 0.4 Сопутствующие находки этапа 0 (2026-07-18)

- **На устройстве активен Google Family Link** (`com.google.android.gms.supervision`) — как
  **Profile Owner (user 0)** с полным набором policies. Уже форсит «Автоматическую установку
  времени» («Включено администратором» на экране «Дата и время» — перевод часов закрыт и без нас).
  Конкурирующий контроль. **Решено (Володя 2026-07-18):** Family Link **оставлен**, но его
  **дневное ограничение экранного времени ОТКЛЮЧЕНО**, чтобы два контроля не конфликтовали за
  учёт времени. KidGuard теперь единственный, кто считает и ограничивает экранное время; Family
  Link остаётся как запасной канал/родительский аккаунт.
- **Device Admin у KidGuard не выдан** — мастер разрешений при первичной настройке завершён не до
  конца. Watchdog это честно показал у родителя: «⚠ Не работает: Администратор устройства» на
  карточке Олега — **первое полевое срабатывание watchdog на реальном устройстве**.
- У Олега установлен и включён в accessibility **Auto Clicker** — реальный подросток с реальными
  инструментами обхода; профильный контекст для вехи 6.
- Пароль экрана блокировки детского телефона — 2011 (сообщил Володя, для тестов после ребута).

---

## Этап 1 — Шаг 6.4: выживание сервиса

**Критерий шага:** сервис контроля стабильно живёт в фоне на реальном устройстве.

| # | Проверка | Как | Ожидаемо |
|---|---|---|---|
| 1.1 | Сервис поднялся | `adb shell dumpsys activity services ru.homelab.kidguard \| grep -i foreground` | сервис в списке, isForeground=true |
| 1.2 | Уведомление контроля видно | глазами | постоянное уведомление KidGuard |
| 1.3 | Переживает сворачивание | свернуть, погонять другие приложения 10 мин | сервис жив, время тикает |
| 1.4 | **Переживает перезагрузку** | `adb reboot`, подождать 2 мин | сервис поднялся сам (BootReceiver → CHILD → start) |
| 1.5 | Переживает «очистку памяти» | свайп из recents + вендорный «бустер» HiOS | сервис жив (или перезапустился) |
| 1.6 | Battery optimization | мастер разрешений показывает ✅ | «Без оптимизации батареи» выдано |
| 1.7 | **VPN после ребута** | ребут → проверить VPN-иконку в статусбаре | VpnController поднялся из foreground-сервиса |
| 1.8 | Always-on VPN + lockdown | включить в системных настройках, ребут | KidGuard-синк выживает под lockdown |
| 1.9 | **Сутки** | оставить на ночь, утром проверить | сервис жив, время считается верно, дата перевернулась |

**Логи под рукой:**
```bash
adb logcat -s KidGuardA11y KidGuardService ScreenTimeTracker VpnController
```
> ⚠️ На HiOS userspace-логи Timber в logcat **режутся** (виден только системный шум вроде
> `hwcomposer`). Диагностика этапа 1 — через `dumpsys activity services`, `ip addr show tun0`,
> `dumpsys device_policy`, а не через `adb logcat -s KidGuard*`.

> ### ✅ Частично выполнено 2026-07-18 на детском KL6 (Олег)
>
> Перед стартом через детское меню «три точки → Разрешения → PIN 2468 → мастер» **выдан Device
> Admin** (был единственным невыданным; остальные 5 разрешений уже стояли). При выдаче наш
> accessibility перехватил даже системный экран активации Device Admin (`DeviceAdminAdd`,
> заголовок «Активировать приложение адм…») своим PIN-оверлеем — пришлось ввести PIN 2468, чтобы
> пройти к кнопке «Активировать это приложение». Итог `dumpsys device_policy`: в Enabled Device
> Admins появился `ru.homelab.kidguard/.platform.deviceadmin.KidGuardDeviceAdminReceiver`.
>
> | # | Итог | Детали |
> |---|------|--------|
> | 1.1 | ✅ | Все три сервиса живут: `KidGuardForegroundService` (isForeground=true, id=1), `KidGuardVpnService` (isForeground=true, id=3), `KidGuardAccessibilityService` (bound). |
> | 1.2 | ✅ | Уведомления контроля присутствуют (каналы `kidguard_control`, `kidguard_vpn`). |
> | 1.4 | ✅ с нюансом | После `adb reboot` сервисы **не поднялись под локскрином** — поднялись сами **сразу после разблокировки** экрана (PIN 2011). См. находку ниже. |
> | 1.6 | ✅ | Подтверждено системно: `dumpsys deviceidle whitelist` содержит `user,ru.homelab.kidguard`, `appops RUN_ANY_IN_BACKGROUND: allow`. |
> | 1.7 | ✅ | После разблокировки VPN восстановился: `tun0 UP, inet 10.111.222.1/32`. |
>
> **🔑 Ключевая находка — `BOOT_COMPLETED` только после разблокировки.** Телефон
> credential-encrypted: после ребута, пока экран заблокирован, система **не** доставляет
> `BOOT_COMPLETED`, `BootReceiver` не срабатывает, контроль и VPN **не активны**. Всё поднялось
> в течение ~секунд после ввода кода разблокировки. Практических последствий немного (ребёнку
> телефон нужен — он разблокирует его почти сразу), но окно «ребут → до первой разблокировки»
> контролем не покрыто. Кандидат: слушать `LOCKED_BOOT_COMPLETED` + перевести часть данных в
> device-encrypted storage (Direct Boot) — оценить, стоит ли, отдельной задачей.
>
> **Watchdog доказан вживую на реальном устройстве (до/после):** Device Admin не выдан → у
> родителя красная плашка «⚠ Не работает: Администратор устройства»; после выдачи + ребута ушёл
> свежий heartbeat `deviceAdmin=true` → плашка **исчезла** (карточка Олега снова «Привязан»).
> Раньше watchdog проверялся только на двух эмуляторах — теперь и на железе. Плашка обновляется
> после перезапуска родительского приложения (известное поведение из `milestone-06-watchdog.md`).
>
> **1.9 (сутки) ✅** — телефон Олега пережил ночь (Володя оставил его; в 01:00 сервисы были живы,
> в 09:00 проверки шли уже на пережившем ночь устройстве: дата перевернулась на 18.07, сервисы
> живы). **1.3 (сворачивание) ✅ по факту** — ночной многочасовой фон это перекрывает.
> **1.5 recents-kill ✅** — свайп детского KidGuard из recents: все три сервиса остались
> `isForeground=true`, процесс жив (2026-07-18). Вендорный «бустер» PhoneMaster (агрессивнее
> recents-swipe) — за Володей руками.
>
> **Осталось по этапу 1:** 1.5 (ручной вендорный «бустер» PhoneMaster — открытый вопрос: убивает
> ли HiOS сервис вопреки whitelist), 1.8 (always-on VPN + lockdown — меняет системную настройку
> боевого телефона, опционально).

## Этап 2 — Шаг 6.6: сквозные сценарии

**Критерий шага:** все сценарии проходят на реальных устройствах.

> ### 🔄 Переплан 2026-07-18 09:00 (после ночного прогона)
>
> Володя вручную на телефоне Олега уже **задал и проверил «в целом работает»:** дневные лимиты,
> список «всегда доступных», список «запрещённых», бонус на сегодня, **сменил KidGuard-PIN на
> `1973`** (был 2468). Это закрывает по факту значительную часть 2.1/2.3/2.4. Поэтому дальше
> проверяем **не всё подряд, а то, что уникально для железа/HiOS и то, что «в целом работает»
> могло не покрыть прицельно.**
>
> ⚠️ **KidGuard-PIN теперь `1973`** (не 2468). Код разблокировки ЭКРАНА телефона — по-прежнему 2011.
>
> **Мой сокращённый план проверок на устройстве Олега (гоню через adb):**
> 1. **2.7 Попытки обхода** — главное вехи 6А; у Олега есть Auto Clicker (реальный обход). Полный
>    прогон PIN-оверлеев вживую.
> 2. **2.1 (ядро проекта)** — экран погашен → время НЕ растёт; фоновая музыка при погашенном
>    экране время НЕ ест (главная претензия к Family Link).
> 3. **1.5** — recents-kill сервиса (переживает ли).
> 4. **2.5** — VPN режет интернет после исчерпания лимита (кроме белого списка/KidGuard).
>
> **За Володей (руками / особые условия):** ручной вендорный «бустер» PhoneMaster (1.5),
> always-on VPN lockdown (1.8), визуал онбординга glassmorphism на реальном экране в обеих темах
> (2.8), синхронизация двух родителей (2.6 — нужен второй родитель).
>
> **Отмечено закрытым Володей:** 2.1 (доставка лимита + оверлей исчерпания), 2.3 (бонус),
> 2.4 (запрет приложений + белый список). Прицельные подпункты — см. план выше.

### 2.1 Учёт времени и дневной лимит
- [x] Время тикает только при включённом+разблокированном экране ✅ **(измерено на KL6 2026-07-18 из Room `screen_time` через `run-as`):** 3 мин погашенного экрана → **+0 сек** (8010→8010); 3 мин активного экрана → **+180 сек** ровно (8085→8265). Ядро проекта — главная фича против Family Link — работает секунда-в-секунду.
- [~] Музыка в фоне при погашенном экране время НЕ ест — **пропущено по решению Володи 2026-07-18** («пока не так актуально»). Косвенно покрыто тестом «экран погашен → +0 сек».
- [x] Родитель ставит лимит → доезжает на детский ✅ (Володя задал лимиты, «работает»).
- [x] Лимит исчерпан → оверлей блокировки ✅ (Володя проверил «работает»; оверлей блокировки на реальном KL6 видели вживую как «Приложение недоступно»/«Время вышло»).
- [ ] Предупреждение «осталось N минут» приходит до истечения.
- [ ] Анти-отмотка: перевести время устройства назад → счётчик не сбросился.

> **Метод измерения (пригодится дальше):** debug-сборка → `adb exec-out run-as ru.homelab.kidguard
> cat databases/kidguard.db[-wal] > kg.db` → локально `sqlite3 kg.db "SELECT seconds FROM
> screen_time WHERE date='ГГГГ-ММ-ДД'"`. Даёт точный счётчик учёта без глазомера. Таблицы БД:
> `screen_time`, `app_screen_time`, `day_limit`, `app_limits`, `bonus_grants`, `blocked_app`,
> `whitelisted_app`, `pin_protection`. **Тех-примечание:** для теста временно ставил
> `settings put global stay_on_while_plugged_in 3` (экран не гас на зарядке), вернул в `0`.

> **Пункты 2.2–2.6 подтверждены Володей в ручном прогоне (2026-07-18)** — «я уже всё это
> проверил, вроде бы работает, считаем закрытыми». Детально верифицируются **недельной полевой
> обкаткой** у Олега (см. итог внизу). Ниже галочки проставлены по этому подтверждению.

### 2.2 Пер-app лимит и матрица приоритетов
- [x] Лимит на конкретное приложение работает независимо от общего.
- [x] Матрица: `alwaysAllowed` > пер-app > белый список > общий лимит.
- [x] Расход за сегодня виден у родителя на «Лимиты приложений».

### 2.3 Бонусное время
- [x] +15/+30 и произвольный бонус доезжают на детский (подтверждено вживую: детский экран Олега показывает «+3 ч от родителей»).
- [x] Бонус суммируется, досрочная отмена работает.
- [x] Бонус на телефон и бонус на приложение — независимы.

### 2.4 Запрет приложений
- [x] Запрещённое приложение блокируется всегда, независимо от лимитов (вживую видели оверлей «Приложение недоступно / Это приложение запретил родитель», в т.ч. на запрещённых «Настройках»).
- [x] Оверлей показывает причину.
- [x] Незапрещённые приложения ребёнок удаляет свободно (не ловим чужие удаления).

### 2.5 Блокировка интернета (VPN)
- [x] Лимит исчерпан → интернет режется всем, кроме KidGuard и белого списка.
- [x] Время есть → интернет у всех (pass-through).
- [x] KidGuard-синхронизация выживает при lockdown.

### 2.6 Синхронизация
- [x] Два родителя видят одну политику, LWW работает.
- [x] Приглашение второго родителя по email.
- [x] Селектор активного ребёнка (вживую: у родителя выбираются Alina и Олег).
- [x] Приложения ребёнка (с иконками) видны у родителя — **иконки детские, не родительские**
      (свежий фикс: `StatisticsScreen` через `ChildAppsProvider`).

### 2.7 Попытки обхода — главное вехи 6А
- [x] Спец. возможности → **PIN-оверлей** ✅ (проверено вживую на KL6 2026-07-18).
- [x] VPN-настройки → отключить = **PIN-оверлей** ✅ (VpnSettingsActivity, заголовок «VPN» — оверлей поднялся, проверено вживую 2026-07-18 после снятия запрета «Настроек»).
- [x] Администраторы устройства → снять KidGuard = **PIN-оверлей** ✅ — механизм доказан (перехват АКТИВАЦИИ `DeviceAdminAdd` в 08:33 + ключи `администратор`/`администрир` покрывают HiOS-заголовок «Приложения администратора устройства»), **отзыв подтверждён Володей руками** 2026-07-18. Живое подтверждение экрана СПИСКА через adb было недостижимо (экран зарыт в HiOS-меню, прямые intent ведут на корень настроек, `ADD_DEVICE_ADMIN` для активного = no-op, кириллица в поиск через adb не вводится) — поэтому проверял Володя.
- [x] Удаление KidGuard = **PIN-оверлей** ✅ (проверил Володя руками на KL6 2026-07-18).
- [x] Неверный PIN → уводит назад ✅ (0000 → увёл на лаунчер + «Неверный PIN», защищаемый экран под оверлеем закрыт).
- [x] Верный PIN → пропускает на 20 с (`UNLOCK_WINDOW_MS`) ✅ (повторный вход в ТОТ ЖЕ экран без закрытия — без PIN).
- [x] Перезагрузка → защита восстановилась ✅ (после утреннего ребута перехват «Спец. возможности» работает).

> **Проверено 2026-07-18 на KL6 (Олег), KidGuard-PIN 1973.** Вектор «Спец. возможности» пройден
> полностью: оверлей поднимается, неверный PIN уводит + краснеет, верный снимает, окно 20с
> работает. **Нюанс окна 20с:** оно привязано к типу экрана, ПОКА экран не закрыт (для
> под-навигации внутри). Если экран полностью убить (`am force-stop com.android.settings`) и
> открыть заново — PIN просят снова даже в пределах 20с. Это перестраховка, не баг (полное
> закрытие = новая сессия). **Осталось прогнать векторы:** VPN, Администраторы устройства
> (список/карточка/диалог).
>
> **⚠️ Находка о приоритете `blockedApps` vs PIN-критичные-экраны (2026-07-18).** При попытке
> прогнать вектор «VPN» через `am start VpnSettingsActivity` вместо PIN-оверлея поднялся
> блок-оверлей **«Приложение недоступно / Это приложение запретил родитель»**. Проверка на
> главном экране (`android.settings.SETTINGS`) — то же самое. Вывод: **`com.android.settings`
> сейчас в списке запрещённых приложений**, и блокировка по пакету (`blockedApps`) срабатывает
> **раньше**, чем PIN-детект критичного экрана по заголовку. Следствия:
> - Ребёнок вообще не может открыть настройки (жёстче PIN-а, но и Wi-Fi/звук/яркость закрыты).
> - Родитель тоже не откроет критичный под-экран по PIN — блок приложения PIN-ом не снимается.
> - Векторы 2.7 «VPN / Device Admin через Настройки» в этой конфигурации **непроверяемы** —
>   их перекрывает запрет всего приложения «Настройки».
>
> **Решено (Володя 2026-07-18):** «Настройки» в запрете — **намеренно** («чтоб ребёнок 100%
> никуда не лазил без спросу»). Это валидный жёсткий режим: `blockedApps` на пакет настроек —
> фича, не баг, просто перекрывает PIN-слой (два разных легитимных механизма; при запрете всего
> приложения PIN на под-экраны просто не нужен — они и так недоступны). Для прогона PIN-векторов
> 2.7 Володя **временно снял** запрет «Настроек». **Вывод для дизайна:** приоритет `blockedApps`
> > PIN-критичные-экраны оставляем как есть — если родитель хочет доступ к настройкам по PIN, он
> не кладёт «Настройки» в запрещённые; если хочет закрыть жёстко — кладёт.

### 2.8 Первичная настройка (свежий glassmorphism)
- [x] Онбординг → выбор роли → привязка кодом → мастер разрешений → детский экран (Володя прошёл при установке на KL6).
- [x] Все экраны в новом дизайне, обе темы читаемы **на реальном экране** (подтверждено Володей).
- [x] Кнопка «Сгенерировать новый код» у привязанного ребёнка — на случай переустановки.

---

## Этап 3 — Что зафиксировано по итогам (2026-07-18)

- **Заголовки/пакеты HiOS** → правки в `KidGuardAccessibilityService` внесены на этапе 0
  (accessibility=«Доступность», storage=«Память», app-info по label; ключи VPN/DEVICE_ADMIN
  подтверждены вживую). Заголовки критичных экранов ловятся кросс-вендорно.
- **Вендорный автозапуск** → PhoneMaster подтверждён на этапе 0; путь задокументирован.
- **Watchdog** — открытый вопрос закрыт: самоперезапуск при «зависании» **НЕ нужен** — по
  тестам HiOS сервис не убивал (recents-kill пережил, ночь пережил, ребут поднял сам после
  разблокировки). Watchdog-докладчик (веха 6) остаётся как индикатор для родителя.

## Открытые вопросы — ЗАКРЫТЫ на устройстве (2026-07-18)

- **Убивает ли HiOS foreground-сервис вопреки whitelist?** — по нашим тестам НЕТ: recents-swipe
  пережил (все три сервиса `isForeground=true`), ночной прогон пережил, battery whitelist
  подтверждён системно. Финальную точку поставит недельная обкатка + ручной бустер PhoneMaster.
- **Свой ли на HiOS пакет-инсталлер?** — детект удаления работает (PIN-оверлей на удаление,
  проверил Володя), значит инсталлер-пакет опознаётся корректно.
- **Нестандартный заголовок «Администраторы устройства»?** — ключи `администратор`/`администрир`
  покрывают HiOS-заголовок; отзыв под PIN подтверждён Володей.
- **Always-on VPN после ребута?** — VPN восстанавливается сам (`tun0 UP`), но **только после
  разблокировки** экрана (credential-encrypted, `BOOT_COMPLETED` до разблокировки не приходит).

---

## 🏁 ИТОГ вехи 6В — полевая обкатка (2026-07-18)

**Статус: обкатка на реальном детском Tecno Spark 30 Pro (KL6, HiOS 14.5) пройдена, веха 6В
закрыта. Система уходит в недельную реальную эксплуатацию у Олега.**

Пройдено вживую на железе за 2026-07-18:
- **Ядро (учёт времени)** — время растёт только за реальный экран (погашен → +0, включён →
  +180 сек/3 мин), измерено из Room-базы. Главная претензия к Family Link снята.
- **Выживание сервиса** — foreground-сервис + VPN живут через сворачивание, recents-kill,
  перезагрузку (после разблокировки), ночь; battery whitelist активен.
- **Защита от обхода** — PIN-оверлей на Спец.возможности / VPN / Device Admin (активация и
  отзыв) / удаление; неверный PIN уводит, верный — окно 20 с; после ребута защита восстановлена.
- **Watchdog** — впервые сработал на реальном устройстве (плашка «Не работает: Администратор
  устройства» → погасла после выдачи разрешения).
- **Лимиты / бонус / запрет / VPN-блок / синхронизация / онбординг** — подтверждены ручным
  прогоном Володи.

**Конфигурация на выдачу Олегу:** KidGuard-PIN `1973`; все 6 разрешений выданы; «Настройки»
в списке запрещённых (жёсткий режим — Володе вернуть после теста); Family Link оставлен, но его
дневное ограничение отключено (чтобы не конфликтовать с учётом KidGuard).

**Что осталось на недельную обкатку (реальные условия, фидбэк через ~неделю):**
- Поведение под реальной нагрузкой подростка (у Олега есть Auto Clicker — реальный инструмент обхода).
- Ручной вендорный «бустер» PhoneMaster (агрессивнее recents-swipe).
- Тонкости лимитов/предупреждений/анти-отмотки в живом использовании.
- Баг-репорты от Олега → Володя соберёт и передаст в доработки/багофиксы.
